#!/bin/sh
# DownloadAll
# XCode 2.2 compliant, version 3
#"$(dirname "$0")/DownloadSparkle.command"
"$(dirname "$0")/DownloadOgreKit.command"
"$(dirname "$0")/DownloadLua.command"
"$(dirname "$0")/DownloadLuaObjcBridge.command"
